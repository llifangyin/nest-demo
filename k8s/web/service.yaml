apiVersion: v1
kind: Service
metadata:
  name: web-svc
  namespace: nest-demo
spec:
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080   # 浏览器访问：http://localhost:30080
  type: NodePort
